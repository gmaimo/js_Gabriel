//* PRIMERA ENTREGA

/* let entrada = Number(prompt(`Introduce la operación que quieres realizar: 1. SUMAR; 2. RESTAR; 3. MULTIPLICAR; 4. DIVIDIR; 5. PORCENTAJE; 6. CONCATENAR:`));
let x = prompt(`Input 1:`)
let y = prompt(`Input 2:`)

function operacion(userInput){
    while (userInput < 1 || userInput > 6 || !Number.isInteger(userInput)){
        return alert(`error 404`);
    }
    if (userInput == 1) return alert(Number(x)+Number(y));
    if (userInput == 2) return alert(x-y);
    if (userInput == 3) return alert(x*y);
    if (userInput == 4) return alert(x/y);
    if (userInput == 5) return alert(x*y/100);
    if (userInput == 6) return alert(`${x} ${y}`);
}

operacion(entrada); */

//* SEGUNDA ENTREGA

/* function input(){
    let precio = prompt(`Introduce el precio de costo`);
    return precio;
}

function calculoIVA(){
    return input() * 1.21;
}

function mostrarTotal(){
    alert(calculoIVA())
}

mostrarTotal(); */

/* function numberInput(){
    let number1 = prompt(`Introduce un número:`);
    return number1;
}

function multipleInput(){
    let number2 = prompt(`Introduce un número para comprobar si el primero es múltiplo de este:`)
    return number2;
}

let num1 = numberInput();
let num2 = multipleInput();

function comprobar(a,b){
    return (a%b == 0)
}

let comprobacion = comprobar(num1,num2);

function output(x){
    if(x == true) return alert (`${num1} es múltiplo de ${num2}`)
    else return alert (`${num1} NO es múltiplo de ${num2}`);
}

output(comprobacion); */

